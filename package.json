{
  "name": "nekopara",
  "version": "0.1.0",
  "description": "",
  "main": "lib/nekopara.js",
  "scripts": {
    "clean": "rm lib -rf",
    "build": "npm run clean && babel src -d lib",
    "test": "npm run build && istanbul cover node_modules/.bin/mocha"
  },
  "author": "shitake",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-preset-es2015": "^6.24.1",
    "chai": "^4.0.2",
    "mocha": "^3.4.2"
  }
}
